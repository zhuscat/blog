import React from 'react'

export default ({ name }) => (
  <div
    style={{
      color: '#8f8f8f',
      marginRight: '8px',
      display: 'inline-block',
      fontSize: '14px',
      borderRadius: '4px',
    }}
  >
    # {name}
  </div>
)
